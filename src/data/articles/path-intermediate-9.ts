import type { IArticle } from '@/types/tutorial';

const article: IArticle = {
  id: 'path-intermediate-9',
  title: 'LoRA模型训练：定制专属风格与人物模型',
  description: '从数据准备、环境配置、训练参数调优到模型测试，完整掌握LoRA轻量级微调技术，训练自己的人物/风格/产品模型。',
  date: '2026-06-01',
  readTime: '12-15 分钟',
  content: `## 为什么需要训练自己的LoRA？

你用AI绘画的时候肯定遇到过这些问题：想画一个特定的人——你自己、你的女朋友、你们公司的IP形象，不管怎么写Prompt、用IP-Adapter都不像，每次画出来脸都不一样；想画特定风格——比如你们品牌的插画风格、某个画家的风格，写了一堆风格词还是不对；想画特定产品——你们公司的产品、某个特定的物品，AI画出来总是歪的、细节不对。

这时候你需要训练自己的LoRA模型。LoRA（Low-Rank Adaptation，低秩适应）是目前最流行的轻量级模型微调技术——你不需要重新训练整个大模型（那要几十万块钱的GPU），只需要用少量图片（10-50张就行）训练一个很小的模型文件（一般几十到几百MB），就能让AI学会画特定的人、特定风格、特定物品。

这是AI绘画从"玩别人的模型"到"做自己的模型"的关键一步。学会训练LoRA之后，你就可以：
- 训练自己的数字分身，用自己的形象生成各种照片
- 训练特定的风格模型，比如你们品牌的专属设计风格
- 训练特定产品模型，生成产品的各种营销图
- 训练特定IP/角色模型，做漫画、动画、游戏
- 甚至可以训练你家猫/狗的LoRA，让它出现在各种场景里

这篇文章我们以Stable Diffusion LoRA训练为例，从数据准备到训练到测试完整走一遍，新手也能跟着训练出自己的第一个LoRA。

## LoRA基本原理（不用懂数学，懂怎么用就行）

你不需要懂LoRA的数学原理，知道这几个关键点就够了：
1. **LoRA是一个"补丁"**：它不是完整的模型，不能单独用，必须挂载在基础大模型（比如SD 1.5、SDXL）上用，像游戏补丁一样，不改变原模型，只是给它加新能力
2. **训练成本极低**：原来训练一个模型要几十张A100跑几天，训练LoRA只需要一张消费级显卡（8GB显存就能跑），10-50张图，训练几十分钟到几小时就够了
3. **效果可控**：训练的时候可以调整网络维度（dim/alpha）控制学习强度，dim越高学习能力越强但越容易过拟合，一般8-64之间就够了
4. **可以叠加**：你可以同时挂载多个LoRA用——比如一个人物LoRA+一个风格LoRA+一个光影LoRA，效果叠加

现在训练LoRA的工具已经非常成熟了，有一键训练包，哪怕你不懂代码、不懂深度学习，照着教程也能训练出能用的LoRA。

## 第一步：准备训练数据

训练LoRA最关键的一步不是调参数，是准备数据集——数据质量决定了你LoRA的上限，垃圾数据训练出来的就是垃圾模型。

### 需要多少张图？
- 训练人物/角色：20-50张图就够了，太多反而容易过拟合
- 训练风格：15-30张风格统一的图
- 训练特定物品/产品：30-100张不同角度、不同背景的图
- 新手不要贪多，先从20张图开始练手，熟练了再加

### 数据要求（非常重要！）
1. **图片质量要高**：不要用模糊的、压缩过的、低分辨率的图，至少要512x512以上，最好1024x1024
2. **多样性足够**：这是新手最容易犯的错——所有图都是同一个角度、同一个背景、同一个表情，训练出来的LoRA只会画这一个角度，换个角度就崩。正确做法是：
   - 不同角度：正面、45度侧面、正侧面、低头、抬头都要有
   - 不同表情：笑、严肃、眨眼、嘟嘴等不同表情
   - 不同背景：不要所有图都是同一个背景，不同场景、不同光线
   - 不同距离：特写、半身、全身都要有
   - 不同衣服/造型：不要都是同一件衣服（除非你就是要训练特定衣服）
3. **不要有多余内容**：图里不要有其他人、不要有太多杂物，主体要突出
4. **风格统一如果训练风格LoRA**：所有图的风格必须高度一致——都是水彩就全是水彩，都是赛博朋克就全是赛博朋克，不要混不同风格的图
5. **不要有水印、文字**：有水印的话AI会学会水印，生成的图也会有水印
6. **尺寸统一**：最好都裁成正方形（1:1比例），或者至少比例一致，训练的时候不要一会横图一会竖图

**人物LoRA数据准备示例（训练你自己）**：
- 找20-30张你自己的清晰照片
- 一半正面一半侧面，各种表情
- 有室内有室外，不同光线（白天、晚上、暖光、冷光）
- 有特写有半身，最好有几张全身
- 不同衣服（不然训练出来AI只会画你那一件衣服）
- 用修图软件把背景里的其他人、杂物裁掉
- 所有图裁剪成1024x1024正方形，你自己在图片中间

### 打标签（Captioning）
每一张图都要配一个文本标签（txt文件），告诉AI这张图里有什么。这一步也很重要，标签写不好训练效果差。

**标签的写法**：
- 用逗号分隔的关键词，和你写Prompt一样
- **通用特征放前面，独特特征放后面，触发词放最前面**
- 每张图共有的特征（也就是你要训练的特征）不要每张都写！不对——刚好相反：
  - 所有图都有的共同特征（比如这个人的名字/你给的触发词、核心特征），在标签里写上，这是LoRA要学习的
  - 每张图不一样的变化特征（衣服、背景、表情、姿势）也要写出来，告诉AI这些是"变化"，不要学

举个例子，训练一个叫"xiaohong"的人物：
- 所有标签第一词都是\`xiaohong\`（这是触发词，用LoRA的时候用这个词触发）
- 每一张图的标签：
  - 正面照：\`xiaohong, 1girl, long black hair, smiling, white t-shirt, looking at viewer, indoor, daylight, close up\`
  - 侧面照：\`xiaohong, 1girl, long black hair, side view, red dress, outdoor, sunset, full body\`
  - 戴眼镜的：\`xiaohong, 1girl, long black hair, wearing glasses, serious expression, office background, medium shot\`

**自动打标签工具**：不用你自己手动一张张写，用工具自动生成标签，你再检查修改：
- **WD14 Tagger**：最常用的二次元/真人都能用的打标签工具，训练工具里一般都内置
- **BLIP/BLIP2**：更擅长自然语言描述
- 操作：先让工具自动打标签，然后手动检查：
  - 删掉错误的标签
  - 加上你认为重要但没识别出来的特征
  - 确保所有标签第一个词是你的触发词
  - 把每张图不同的特征（衣服、背景、表情）补全

### 文件夹结构
训练工具一般要求这样的文件夹结构：
\`\`\`
my_lora/
├── 100_xiaohong/          # 名字随便，前面数字是重复次数（一般100-200）
│   ├── img001.png
│   ├── img001.txt         # 和图片同名的标签文件
│   ├── img002.png
│   ├── img002.txt
│   └── ...
└── log/                   # 训练日志，自动生成
\`\`\`

## 第二步：搭建训练环境

现在训练LoRA有非常简单的方式，不用自己配Python环境、装CUDA，新手推荐用一键包：

### 方案一：云端训练（新手首推，不用本地显卡）
如果你本地没有好显卡（没有N卡，或者显存小于8GB），直接用云端训练，几块钱就能训练一个LoRA：
1. **Kaggle/Colab**：免费的GPU，有现成的LoRA训练Notebook，直接照着跑
2. **AutoDL/矩池云**：国内GPU云平台，按小时租，一张3090一小时才一两块钱，搜"LoRA训练"有现成镜像，一键启动
3. **C站（Civitai）在线训练**：最方便，上传图片直接训练，不用管环境，就是要排队

云端训练的好处：不用自己配环境、显卡够快、不占本地资源，新手强烈推荐先用云端练手。

### 方案二：本地训练（有好显卡的话）
如果你有一张N卡，显存8GB以上，本地训练也很方便：
1. 用**Kohya_ss GUI**：现在最流行的LoRA训练图形界面，一键安装，不用敲命令
2. 去GitHub搜bmaltais/kohya_ss，照着README下载安装包一键安装
3. 支持SD 1.5和SDXL LoRA训练，图形界面点按钮就行，不用写代码

## 第三步：训练参数配置

打开Kohya_ss（或者云端训练界面），这几个是你需要调整的核心参数，其他用默认值就行。我们以训练真人LoRA（SD 1.5模型）为例：

### 基础参数
- **Pretrained Model**：选基础模型，真人用\`chilloutmix\`或者\`realisticVision\`，二次元用\`AnythingV5\`或者你常用的大模型
- **Train Type**：选LoRA（不要选LoCon、LoHa，先用最基础的LoRA）
- **Resolution**：和你图片尺寸一致，一般\`512,512\`（SD 1.5）或者\`1024,1024\`（SDXL）
- **Batch Size**：根据显存选，8GB显存选1-2，12GB选2-4，24GB选4-8
- **Epoch**：训练多少轮。一般10-20轮就行，太多轮会过拟合
- **Save every N epochs**：每N轮保存一个版本，方便你挑效果最好的，比如设2，就是每2轮存一个
- **Learning Rate**：学习率，最关键的参数之一：
  - 真人LoRA：\`1e-4\`（0.0001）
  - 风格LoRA：\`2e-4\`到\`5e-4\`
  - 物品LoRA：\`1e-4\`
  - 新手不要用太大的学习率，容易练坏
- **LoRA Dim (Rank)**：网络维度，控制学习能力：
  - 简单的（风格、简单物品）：8-16
  - 真人/复杂角色：32-64
  - 不要超过128，太大容易过拟合
- **Alpha**：一般设成和Dim一样，或者Dim的一半，新手就设成和Dim一样
- **Scheduler**：学习率调度器，用\`cosine_with_restarts\`或者\`constant\`
- **Optimizer**：选\`AdamW8bit\`，省显存，效果好

### 数据集参数
- **Dataset Directory**：选你放图片的文件夹
- **Num Repeats**：每张图片每个Epoch重复多少次，一般100-200，这个数乘以图片数除以Batch Size就是每个Epoch的步数
- **Caption Extension**：\`.txt\`
- **Shuffle Caption**：勾选，打乱标签顺序，防止模型记住顺序
- **Keep Tokens**：1，就是保持第一个词（触发词）永远在最前面

### 其他参数
- **Mixed Precision**：选\`fp16\`，省显存
- **Gradient Checkpointing**：勾选，省显存（训练会慢一点，但能跑起来）
- **Use EMA**：勾选，训练出的模型更平滑
- **Clip Skip**：二次元选2，真人选1

这些参数新手可以直接抄，训练几个就有感觉了，再自己慢慢调。

### 开始训练
1. 所有参数设置好，填好输出文件夹
2. 点"Start Training"按钮，开始训练
3. 你会看到损失（loss）值慢慢下降——一开始快速下降，后面慢慢趋于平稳，这是正常的
4. 如果loss一直降不下来，或者反而上升，说明学习率太大或者参数错了，停下来调参数
5. 训练完成后，在输出文件夹里会有多个.safetensors文件（每N轮保存一个）

## 第四步：测试和调优

训练完了不是结束，你需要测试每个保存的版本，挑效果最好的，不行还要调参重练。

### 怎么测试？
1. 打开Stable Diffusion WebUI
2. 把生成的.safetensors文件放到\`models/Lora\`文件夹，点刷新
3. 生成一张图，Prompt里这样写：
   \`\`\`
   <lora:你的lora文件名:权重> xiaohong（触发词）, 1girl, long black hair, white t-shirt, standing in street, smiling, looking at viewer, realistic photography, 8k, high detail
   Negative prompt: ugly, deformed, blurry
   \`\`\`
4. LoRA权重从0.6开始试，然后试0.8、1.0，太高容易过拟合
5. 每个Epoch保存的版本都试一下，看哪个版本最像、最自然

### 判断LoRA好不好的标准
1. **像不像**：核心——是不是你训练的那个人/风格/物品？特征有没有学到？
2. **泛化能力**：换衣服、换背景、换姿势还像不像？如果只能画训练图里的衣服背景，那就是过拟合了
3. **灵活性**：能不能和其他风格、其他LoRA配合？权重低一点能不能融入，权重高一点效果明显？
4. **有没有崩坏**：有没有奇怪的伪影、畸形、颜色问题？

### 常见问题及解决方法

**问题1：不像，训练了半天还是认不出来**
原因：训练不够、学习率太低、数据不够、触发词没学好
解决：
- 增加训练Epoch（从10加到20）
- 稍微调大学习率（从1e-4调到2e-4）
- 检查数据：是不是图片质量差？是不是没有突出主体？
- 检查标签：是不是触发词每张都在第一个位置？
- 测试的时候把LoRA权重从0.8调到1试试

**问题2：太像了，但换个姿势/衣服就崩，这是过拟合**
过拟合就是AI死记硬背了训练图片，只会画训练图里的内容，换个东西就不会了，这是新手最容易遇到的问题。
解决：
- 减少训练Epoch，用更早保存的版本（比如训练了20轮，10轮的版本反而更好）
- 降低学习率
- 增加训练数据的多样性——更多角度、更多衣服、更多背景
- Dim调低一点（从64降到32）
- 测试的时候降低LoRA权重（从1降到0.6-0.8）

**问题3：脸像了，但脸总是糊的/畸形的/手崩**
解决：
- 训练数据里加几张清晰的脸部特写
- 增加数据集多样性，不要都是模糊的图
- 测试的时候用更高清的模型，加上高清修复（Hires.fix）
- 用Inpaint修手修脸，我们之前讲过

**问题4：画风不对，把基础模型的画风带偏了，生成的图总是一种奇怪色调**
解决：
- 训练的时候用对基础模型——你用哪个基础模型训练的，生成的时候也要用同一个基础模型
- 降低LoRA权重
- 增加训练数据，不要所有图都是同一个色调
- 风格LoRA容易出现这个问题，适当降低学习率

### LoRA使用进阶技巧
1. **权重调整**：权重不是越高越好！一般0.6-0.8就够了，太高会过拟合、画面崩坏
2. **多LoRA叠加**：你可以同时加载多个LoRA，比如：
   \`\`\`
   <lora:xiaohong:0.7> <lora:film_grain:0.5> <lora:cinematic_lighting:0.6>
   \`\`\`
   人物LoRA权重高一点，风格/光影LoRA权重低一点，效果叠加
3. **触发词一定要写**：你训练的时候定义的触发词一定要写在Prompt里，不然LoRA不会触发，白加了
4. **和ControlNet配合**：LoRA负责"画谁/什么风格"，ControlNet负责"姿势/构图/线稿"，两者配合天衣无缝
5. **负向Prompt很重要**：基础模型的负向Prompt该用还是要用，不要以为加了LoRA就不用负向Prompt了

## LoRA应用场景与商业价值

学会训练LoRA之后，很多商业场景你都能做了：

### 1. 电商应用
- 训练产品LoRA：把你们公司的产品（包包、衣服、家具）训练成LoRA，然后生成各种场景图——模特拿着/穿着、放在客厅、户外使用... 不用拍产品图了
- 训练模特LoRA：签约一个模特拍30张照片，训练LoRA，之后永远可以用这个模特生成各种试穿图，不用每次付费拍摄
- 成本从几万块钱一次拍摄，降到几块钱训练一次，生成一张图几秒钟

### 2. 内容创作
- 个人IP/数字人：训练自己的LoRA，生成各种头像、海报、视频封面，都是你的形象
- 漫画/动画：训练主角的LoRA，每个分镜主角都是同一个人，不会画崩
- 自媒体：训练特定风格的LoRA，所有配图统一风格，形成品牌识别

### 3. 设计行业
- 品牌风格LoRA：把你们品牌的设计风格（插画、海报）训练成LoRA，之后所有设计都是统一品牌风格
- 素材生成：训练特定材质/元素的LoRA，需要的时候直接生成

### 4. 兴趣爱好
- 训练你家宠物的LoRA——让你家猫出现在卢浮宫、在太空漫步、当总统
- 训练你喜欢的动漫角色/游戏角色LoRA，生成各种同人图
- 训练艺术大师风格LoRA——梵高、莫奈、宫崎骏风格，用大师风格画任何东西

## 避坑指南：新手常犯的10个错误

1. **数据太少还都是同角度**：20张图全是正面自拍，训练出来AI只会画正面，转脸就崩。一定要多角度多场景
2. **图片质量差**：用模糊的、微信压缩过的、带水印的图训练，出来的效果肯定差
3. **学习率开太大**：上来就用1e-3，训练几轮就过拟合了，从小的学习率开始试
4. **训练太久**：训练100个Epoch，肯定过拟合，10-20轮足够了
5. **标签没写对**：触发词没每张都放、没区分共性和变化特征，AI不知道学什么
6. **基础模型选错**：真人用二次元基础模型训练，怎么练都不对
7. **测试的时候权重拉满**：权重1.0，出来的图奇怪就说LoRA练坏了，先试试0.6-0.8
8. **Dim设太大**：上来就Dim 128/256，分分钟过拟合，人物32-64足够了
9. **不保存中间版本**：只存最后一轮，过拟合了也没早版本可以用，一定要每几轮存一个
10. **一次失败就放弃**：LoRA训练是个调参的过程，第一次失败很正常，分析问题调整参数再练，练3-5次你就能练出很不错的LoRA了

## 下一步行动建议
不要看了就完了，今天就动手练：
1. 找20-30张照片——就用你自己的照片吧，最方便
2. 注册一个AutoDL或者用Colab，准备好环境
3. 裁剪图片、用WD14打标签，整理成数据集
4. 照着我给的参数训练第一个LoRA
5. 测试每个版本，看看效果，分析问题
6. 第一个肯定不完美，调整参数再练一次，第二次就会好很多

训练LoRA是AI绘画的分水岭——从"用别人做的东西"到"自己创造模型"。当你成功训练出第一个和你自己/你喜欢的角色/你家宠物一模一样的LoRA，生成各种好玩图片的时候，那种成就感是单纯用别人模型比不了的。而且这个技能的商业价值非常高，现在电商、内容行业对LoRA训练的需求极大，会这个你甚至可以接外包赚钱。不用怕难，现在工具已经非常傻瓜化了，新手半天就能跑通第一个模型，赶紧动手试试吧。`,
};

export default article;
