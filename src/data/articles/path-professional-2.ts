import type { IArticle } from '@/types/tutorial';

const article: IArticle = {
  id: 'path-professional-2',
  title: 'RAG 检索增强生成：从入门到生产落地',
  description: 'RAG完整原理、文档解析切片、Embedding模型选择、向量数据库选型、检索策略优化、评估方法，带完整代码示例。',
  date: '2026-05-18',
  readTime: '12-15 分钟',
  content: `## 为什么RAG是目前落地最广的AI技术

RAG就像开卷考试——AI不用死记硬背所有知识，遇到问题先翻书（检索知识库），找到相关内容再组织答案。

你有没有遇到过这些问题？
- 问ChatGPT你们公司的产品怎么定价，它不知道，因为这些私有信息它训练数据里没有
- AI回答公司内部流程问题，经常一本正经胡说八道，因为它没看过你们的文档
- 想做一个智能客服，但是产品信息、售后政策经常变，微调模型成本太高，更新也慢
- 用GPT-4长上下文窗口把所有文档塞进去，但是Token贵死，而且文档一多准确率反而下降

这些问题，RAG都是目前最好的解决方案。2026年的今天，RAG已经是企业级AI应用的标配技术——企业知识库、智能客服、文档问答、法律/医疗/金融专业问答，几乎所有需要"基于私有数据回答问题"的场景，都在用RAG。

和微调（Fine-tuning）比，RAG有几个巨大优势：
1. **知识实时更新**：改了文档不用重新训练模型，更新知识库就行，分钟级生效
2. **成本低**：不用花几万块微调模型，搭个向量数据库就行
3. **可解释**：AI回答会标注来源是哪份文档哪一页，用户可以去看原文，不用担心幻觉
4. **不需要训练数据**：直接用现成的文档，不需要标注数据

**这篇文章适合谁看？**
- **开发者**：想知道RAG系统怎么从零搭建、代码怎么写、生产环境怎么优化
- **企业IT/数字化负责人**：想搭企业知识库、智能客服，理解技术选型和成本
- **产品经理**：理解RAG的能力边界和优化方向，设计更好的AI产品
- **创业者**：做RAG相关产品，知道行业最佳实践和坑

这篇文章会从原理讲起，到文档处理、Embedding、向量数据库、检索策略、评估、优化、代码示例，覆盖RAG从入门到生产落地的全流程。

## RAG基本原理：三步走

RAG（Retrieval-Augmented Generation，检索增强生成）的原理非常简单，就三步：

\`\`\`
用户提问 → 检索（Retrieval）→ 增强（Augmentation）→ 生成（Generation）→ 回答
\`\`\`

**第一步：索引阶段（离线做一次）**
1. 把你的文档（PDF、Word、飞书文档、网页、数据库记录...）加载进来
2. 把长文档切分成一小块一小块的（Chunking），一般每块500-1000字
3. 用Embedding模型把每个文本块转换成向量（一串数字，代表文本的语义）
4. 把向量存到向量数据库里，建立索引

**第二步：查询阶段（在线实时做）**
1. 用户提问，把用户的问题也用同一个Embedding模型转成向量
2. 在向量数据库里搜索"和问题向量最相似"的文本块（Top-K检索）
3. 把检索到的相关文本块和用户问题一起塞进Prompt："根据以下参考资料回答用户问题：{检索到的内容} 用户问题：{用户问题}"
4. 让大模型根据参考资料生成答案，返回给用户

就是这么简单！核心思想就是"不要让模型瞎编，先给它找参考资料，让它根据资料回答"，这样既解决了模型不知道私有数据的问题，又大幅减少了幻觉。

你可能会问：直接把所有文档塞到上下文窗口里不行吗？现在模型都支持128K甚至100万Token了。答案是不行，因为：
- **成本问题**：每次查询都传大量Token，费用是按Token计费的，长期下来很贵
- **噪音问题**：文档里大部分内容和问题无关，塞太多无关信息反而会让模型分心（Lost in the Middle问题），准确率下降
- **长度限制**：再长的上下文也有上限，企业文档动辄几百上千份，根本塞不下
- **速度问题**：上下文越长推理越慢，用户体验差

RAG的精髓在于"精准检索"——只把和问题最相关的那几块内容给模型，既省钱又快又准。

## 第一步：文档加载与解析

RAG效果好不好，一半取决于文档处理。垃圾进垃圾出，文档处理得差，后面检索和生成再厉害也没用。

### 支持的文档格式
不同格式需要不同的加载工具，常用的：
- **PDF**：PyMuPDF（fitz）、pdfplumber（对表格友好）、Unstructured
- **Word/Docx**：python-docx、mammoth
- **Excel/CSV**：pandas、openpyxl
- **网页HTML**：BeautifulSoup、Playwright（JS渲染页面用这个）
- **Markdown**：直接读文本就行
- **飞书/钉钉/Notion文档**：用官方API导出
- **图片/扫描件**：需要先用OCR（PaddleOCR、Tesseract、或者多模态模型如GPT-4o）

**坑**：PDF是最麻烦的格式，特别是扫描版PDF、有复杂表格的PDF、双栏排版的PDF，一定要测试解析效果，不要假设加载进来就是对的。

## 第二步：文本分块（Chunking）——RAG效果最关键的一环

文档加载完是很长的文本，必须切分成小块才能向量化和检索。分块策略直接决定检索质量——块太大噪音多，块太小丢失上下文。

### 常用的分块策略：

**1. 固定长度分块**
最简单，按固定字符数/Token数切分，比如每块500字，相邻块重叠50-100字（保证上下文连贯）。
- 优点：实现简单、速度快
- 缺点：容易把完整的语义切断（比如一个段落从中间切开）
- 适合：纯文本、结构简单的文档

**2. 按语义分块（Semantic Chunking）**
不是按固定长度，而是按语义完整性切分——在意思转变的地方切。怎么判断意思转变？计算相邻句子的Embedding相似度，相似度骤降的地方就是切分点。
- 优点：每个块都是一个完整的语义单元，检索质量高
- 缺点：计算量大一些，需要调参
- 适合：大多数场景，生产环境推荐

**3. 按结构分块（Structural Chunking）**
按文档本身的结构切分：按标题、段落、列表、表格等Markdown/HTML结构切。
- 优点：最自然，每个块有完整的结构
- 缺点：需要文档有清晰的结构，格式混乱的文档不好用
- 适合：有良好结构的文档（Markdown、HTML、规范的Word）

**4. 分层分块（Hierarchical Chunking）**
同时存小块和大块：小块（200字）用于精准检索，检索到小块后，再取它所属的大块（1000字）给模型，这样既有检索精度又有上下文完整性。这是目前生产环境非常推荐的方案。

### 分块参数推荐
- **块大小**：512-1024 Token（约300-600中文字），通用场景用512
- **重叠长度**：块大小的10-20%（50-100 Token），保证上下文不丢失
- **特殊处理**：表格不要切碎、代码块整体保留、列表项尽量完整

**分块的黄金法则**：一个块应该能独立回答一个具体的问题。如果一个块被检索到，但读它的人不知道在说什么、缺少上下文，那你的分块就有问题。

## 第三步：Embedding模型选择

Embedding模型把文本转换成向量，向量的质量直接决定检索的准确率——意思相近的文本向量应该距离近，不相关的向量应该距离远。

### 开源 vs 闭源Embedding模型

| 模型 | 维度 | 中文效果 | 速度 | 成本 | 推荐场景 |
|------|------|---------|------|------|---------|
| OpenAI text-embedding-3-small | 1536 | 好 | 快 | 便宜 | 快速原型、英文为主 |
| OpenAI text-embedding-3-large | 3072 | 很好 | 中 | 中等 | 对效果要求高、英文为主 |
| bge-large-zh-v1.5（开源） | 1024 | 很好 | 快（本地） | 免费 | **中文场景首选，本地部署** |
| bge-m3（开源多语言） | 1024 | 好 | 中 | 免费 | 多语言场景 |
| m3e-base/large（开源） | 768/1024 | 好 | 快 | 免费 | 中文开源备选 |
| Cohere embed-v3 | 1024 | 好 | 快 | 便宜 | 英文场景备选 |

**中文场景强烈推荐bge-large-zh-v1.5**——这是目前中文开源Embedding模型的标杆，效果和OpenAI的模型差不多甚至更好，而且可以本地部署，不花钱、数据不出去、延迟低。

### 本地部署Embedding
用Ollama或者FastEmbed可以非常方便地本地部署Embedding模型：

\`\`\`python
from fastembed import TextEmbedding

model = TextEmbedding(model_name="BAAI/bge-large-zh-v1.5")
embeddings = list(model.embed(["你的文本"]))
\`\`\`

几行代码就搞定，不需要GPU也能跑（当然有GPU更快）。

## 第四步：向量数据库选型

向量数据库专门用来存储向量和做相似度搜索，千万不要用传统关系数据库凑合——10万条以上向量，传统数据库的检索速度会让你崩溃。

### 常用向量数据库对比

| 数据库 | 类型 | 易用性 | 规模 | 特点 | 推荐场景 |
|--------|------|--------|------|------|---------|
| Chroma | 嵌入式 | 极简单 | 小（<10万） | 零配置，pip install就能用 | **原型开发、小项目** |
| FAISS | 库 | 简单 | 中（百万级） | Meta开源，速度极快，只是一个库没有服务 | 研究、不需要服务化 |
| Qdrant | 服务 | 简单 | 大（亿级） | Rust写的，性能好，功能全，开源 | **生产环境首选** |
| Milvus/Zilliz | 服务 | 中等 | 超大（十亿级） | 分布式，适合超大规模 | 企业级超大规模 |
| Pinecone | 云服务 | 简单 | 大 | 全托管，不用运维 | 不想自己运维 |
| Weaviate | 服务 | 中等 | 大 | 功能多，支持混合搜索 | 功能需求多 |
| pgvector | PG扩展 | 简单 | 中 | PostgreSQL扩展，如果你已经用PG | 中小规模、已有PG |

**我的推荐**：
- 做原型/小项目：Chroma，五分钟跑起来
- 生产环境中小规模：Qdrant，开源免费、性能好、好部署（Docker一行命令）
- 已经用PostgreSQL：pgvector足够了，不用额外引入新数据库
- 超大规模（千万级以上）：Milvus

## 第五步：检索策略——从简单到高级

基础的相似度搜索只是入门，要做好RAG检索，你需要掌握这些高级检索策略：

### 1. 混合检索（Hybrid Search）
纯向量搜索擅长语义匹配（比如用户说"怎么改密码"能匹配到"如何重置账户密码"），但不擅长精确关键词匹配（比如搜具体的产品型号、错误代码）。混合检索=向量语义搜索 + 关键词搜索（BM25），两路结果合并排序，效果比单一搜索好很多。

大多数生产RAG系统都应该开混合检索，Qdrant、Weaviate、Milvus、Elasticsearch都原生支持。

### 2. 重排序（Reranking）
初步检索（向量+BM25）召回Top-20~50个候选块，然后用一个更强大的重排序模型（Reranker）对这20个块做精细排序，取最相关的Top-3~5个给大模型。

Reranker虽然比Embedding慢，但只对几十个候选块做，速度完全可以接受，而效果提升非常明显——检索准确率能提升10-30%。

**推荐Reranker模型**：
- bge-reranker-large（中文开源，首选）
- bge-reranker-v2-m3（多语言）
- Cohere Rerank（英文闭源，效果好）

加一层Rerank是提升RAG效果性价比最高的手段，没有之一。

### 3. 查询改写（Query Rewriting）
用户的提问往往很简短、口语化，直接用来检索效果不好。比如用户问"多少钱"，系统根本不知道他在问什么产品多少钱。查询改写就是让大模型先把用户问题改写成更适合检索的查询：
- 补充上下文（指代消解：把"它"、"这个"改成具体的名词）
- 扩展同义词和相关词
- 分解复杂问题成多个子查询（比如"对比A和B的优缺点"拆成"A的优缺点"和"B的优缺点"两个查询分别检索）

这一步对提升检索召回率非常有帮助，尤其是用户问题很简短或者很复杂的时候。

### 4. 元数据过滤（Metadata Filtering）
给每个文本块加上元数据：来源文档、分类标签、创建时间、权限级别、部门...检索时先按元数据过滤再做相似度搜索，比如"只搜索产品部的文档"、"只搜索最近一年的文档"，既提升准确率又减少搜索范围。

## 第六步：生成答案——Prompt设计

检索到相关内容后，要把内容组织成Prompt给大模型生成答案。一个好的RAG Prompt应该包含：

\`\`\`
你是一个专业的问答助手。请严格根据下面提供的参考资料回答用户的问题。
要求：
1. 答案必须完全基于参考资料，不要使用你自己的知识编造内容
2. 如果参考资料里没有答案，明确说"根据现有资料无法回答这个问题"，不要瞎编
3. 回答要简洁、准确、结构化，用中文回答
4. 在答案末尾标注信息来源（来自哪份文档）

参考资料：
{context}

用户问题：{question}
\`\`\`

**关键要点**：
- 明确告诉模型"只能根据参考资料回答，没有就说不知道"——这是减少幻觉最有效的方法
- 把最相关的内容放在前面和后面（模型对开头和结尾的内容注意力最强，中间容易被忽略，这就是Lost in the Middle问题）
- 给每个参考块加上编号和来源，让模型引用的时候标来源

## 第七步：评估RAG系统效果

RAG系统搭起来很容易，做好很难。你必须有评估方法才能持续优化。评估RAG需要看两个维度：

### 检索质量评估
- **召回率（Recall）**：用户问题的正确答案是否在检索回来的Top-K块里？
- **精确率（Precision）**：检索回来的块有多少是真的和问题相关的？

怎么评估？准备一个测试问答集（至少100个问题，每个问题标注应该命中哪些文档块），然后自动化跑检索，看召回率和精确率。

### 生成质量评估
- **答案正确性**：答案是否准确回答了问题？有没有幻觉？
- **忠实度（Faithfulness）**：答案是不是完全基于参考资料，有没有编造？
- **相关性**：答案和用户问题相关吗？
- **完整性**：答案是否完整，有没有遗漏重要信息？

生成评估可以用大模型自动评估（让GPT-4当裁判给答案打分），配合人工抽样检查。

### 推荐的评估框架
- **RAGAS**：最流行的RAG评估框架，自动化算Faithfulness/Answer Relevance/Context Recall/Context Precision等指标
- **TruLens**：可观测性+评估一体化
- **LangSmith**：LangChain官方的评估和调试平台

**不要凭感觉优化**——一定要有量化指标，每改一个策略（换Embedding、调分块参数、加Rerank），跑一下评估看指标升了还是降了，用数据说话。

## 生产环境RAG优化清单

当你的RAG系统要上生产给真实用户用，这些优化一定要做：

1. **加Reranker**：bge-reranker-large，效果提升最明显
2. **混合检索**：向量+BM25关键词，不要只用向量搜索
3. **查询改写**：用HyDE或者多查询生成提升召回
4. **分层索引**：小块检索+大块上下文，或者父子文档策略
5. **块元数据**：每个块带上来源、时间、分类、权限等信息，支持过滤
6. **缓存**：高频问题的答案缓存起来，不用每次都检索+生成，速度快10倍
7. **流式输出**：答案流式返回给用户，首Token延迟控制在1秒内
8. **并发和限流**：控制Embedding和LLM的并发，避免被API限流
9. **错误降级**：LLM/Embedding服务挂了要有降级方案
10. **日志和监控**：记录每个查询、检索结果、生成答案、用户反馈，持续优化

## 完整代码示例

用LangChain + Qdrant + bge-large-zh-v1.5搭一个最小可用RAG：

\`\`\`python
from langchain_community.document_loaders import DirectoryLoader
from langchain_text_splitters import RecursiveCharacterTextSplitter
from langchain_community.vectorstores import Qdrant
from langchain_community.embeddings import FastEmbedEmbeddings
from langchain_community.chat_models import ChatOpenAI
from langchain.chains import RetrievalQA
from qdrant_client import QdrantClient

# 1. 加载文档
loader = DirectoryLoader("./docs", glob="**/*.md")
docs = loader.load()

# 2. 分块
text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=512, chunk_overlap=50
)
chunks = text_splitter.split_documents(docs)

# 3. 创建向量数据库（本地bge-large-zh-v1.5嵌入）
embeddings = FastEmbedEmbeddings(model_name="BAAI/bge-large-zh-v1.5")
client = QdrantClient(path="./qdrant_db")
vectorstore = Qdrant.from_documents(
    chunks, embeddings,
    collection_name="my_docs",
    location=":memory:"  # 生产环境换成Qdrant服务地址
)

# 4. 创建RAG问答链
qa = RetrievalQA.from_chain_type(
    llm=ChatOpenAI(model="gpt-4o-mini", temperature=0),
    chain_type="stuff",
    retriever=vectorstore.as_retriever(search_kwargs={"k": 4}),
    return_source_documents=True,
)

# 5. 提问
result = qa.invoke("我们公司的请假流程是什么？")
print(result["result"])
for doc in result["source_documents"]:
    print(f"来源：{doc.metadata['source']}")
\`\`\`

这只是基础版本，生产环境还需要加Reranker、查询改写、混合检索、Prompt优化等等。

## 常见坑和避坑指南

**Q1：答案经常出现幻觉（编造不存在的信息）怎么办？**
A：
1. Prompt里明确强调"只能根据参考资料回答，没有就说不知道"
2. 降低temperature到0-0.1，减少创造性
3. 提升检索质量——很多幻觉是因为检索到的内容不对，模型只能瞎编
4. 加Reranker提高检索精度
5. 加答案验证步骤：让模型检查自己的答案有没有超出参考资料范围

**Q2：检索不到相关内容，召回率低怎么办？**
A：
1. 检查分块策略，是不是块太大或太小
2. 换更好的Embedding模型（中文用bge-large-zh-v1.5）
3. 加查询改写，把用户问题改写成更适合检索的形式
4. 用混合检索（向量+关键词）
5. 增加Top-K数量（比如从4加到8-10）

**Q3：RAG系统响应太慢怎么办？**
A：
1. 首Token延迟控制：用流式输出、LLM用更快的模型（gpt-4o-mini比gpt-4o快很多）
2. Embedding本地部署，不要调用云端API，减少网络延迟
3. 缓存高频问题
4. 向量数据库和应用部署在同一个机房
5. 检索并行化

**Q4：文档更新了怎么同步到知识库？**
A：
1. 做增量更新机制，不要每次重建全量索引
2. 文档有唯一ID，更新时删除旧块加入新块
3. 定期重建索引（比如每周一次），清理碎片和优化索引
4. 用文档的hash判断是否变化，没变就跳过

**Q5：多模态文档（有图片、图表）怎么处理？**
A：
1. 用多模态模型（GPT-4o、Qwen-VL）对图片做描述，把图片描述文本也存入索引
2. 表格单独处理，用HTML格式或者Markdown表格格式存储，不要转成纯文本
3. OCR提取扫描图片中的文字

RAG看起来简单，就是"搜一下再回答"，但要真正做好，每个环节——文档解析、分块、Embedding、检索、Rerank、生成、评估——都有大量细节需要打磨。这篇文章给你的是完整的知识地图和生产级经验，按照这个路径一步步走，你就能搭出真正能用的RAG系统。最好的学习方式是动手：找一份你们公司/你自己的文档，照着代码示例搭一个最小RAG，然后一步步优化，用RAGAS评估效果，你会在这个过程中学到最多。`,
};

export default article;
